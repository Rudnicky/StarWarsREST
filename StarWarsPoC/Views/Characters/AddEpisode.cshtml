@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@model StarWarsPoC.Core.ViewModels.AddEpisodeToCharacterViewModel

@{
}

<h2>Add Episode to @Model.Character.Name</h2>

<form asp-controller="Characters" asp-action="AddEpisode" method="post">
    <div class="form-group">
        <label asp-for="EpisodeId"></label>
        <select asp-for="EpisodeId" asp-items="Model.Episodes"></select>
        <span asp-validation-for="EpisodeId"></span>
    </div>

    <input type="hidden" name="CharacterId" value="@Model.Character.Id" />

    <input type="submit" value="Add Item" />
</form>